# requirements.txt — Sample Website Builder
# Layer 3 execution dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# For an isolated environment (recommended):
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt

# ── HTTP & Scraping ───────────────────────────────────────────────────────────
requests==2.32.3          # HTTP client used in all execution scripts
beautifulsoup4==4.12.3    # HTML parsing for scraping directives
lxml==5.2.2               # Fast parser backend for BeautifulSoup

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv==1.0.1      # Load variables from .env into os.environ

# ── Data Handling ─────────────────────────────────────────────────────────────
# Uncomment as needed when adding new execution scripts:

# openai==1.30.1          # OpenAI API (add when LLM execution scripts are added)
# pymongo==4.7.2          # MongoDB (add when database scripts are added)
# supabase==2.4.3         # Supabase (add when Supabase scripts are added)
# stripe==9.9.0           # Stripe (add when billing scripts are added)
